import { fetchAllContacts, addContact, deleteContact, updateContact } from "./api";
import { IContact } from "./types";

export type {
    IContact,
}

export {
    fetchAllContacts,
    addContact,
    deleteContact,
    updateContact
}
